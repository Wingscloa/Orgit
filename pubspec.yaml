name: my_awesome_namer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1


environment:
  sdk: ^3.1.1

dependencies:
  http: ^0.13.3
  flutter:
    sdk: flutter
  
  english_words: ^4.0.0
  provider: ^6.0.0
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  google_sign_in: ^6.2.1
  icons_plus: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true


  assets: 
    - assets/
